/*
 ============================================================================
 Name        : cudatbase.cu
 Author      : Peter Mogyorosi
 Version     :
 Copyright   : GPLv2
 Description : CUDA compute reciprocals
 ============================================================================
 */

#include <iostream>
#include <numeric>
#include <stdlib.h>
#include "select.h"



int main(void)
{
	SELECT testSelect;
	//testSelect.loadDatabase();
	//testSelect.showDatabase();

	CSVReader reader("/home/freeart/MscThesis/CuDatBase/cudatbase/src/honda_test.csv",";");
	std::vector<std::vector<std::string> > data = reader.getData();

	    // Print the content of row by row on screen
	    for(std::vector<std::string> vec : data)
	    {
	        for(std::string data : vec)
	        {
	            std::cout<<data << " ; ";
	        }
	        std::cout<<std::endl;
	    }
}
